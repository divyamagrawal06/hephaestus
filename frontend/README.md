# Hephaestus — Frontend

Next.js (App Router) + React + TypeScript + Tailwind CSS dashboard.

## Screens (To Build)
1. Fleet Overview — Asset risk heatmap, anomaly trends
2. Incident Workbench — Root-cause graph, plan cards, constraint panel
3. Simulation Panel — Plan A/B/C comparison curves
4. Report & Audit View — Decision timeline, agent traces

## Setup
```bash
npm install
npm run dev
```
